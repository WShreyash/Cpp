#include <iostream>
using namespace std;

int main()
{
    /*string cars[] = {"Porshe 911", "BMW M5", "Bugatti"};

    // Loop through an array
    for (int i = 0; i < 3; i++)
    {
        cout << cars[i] << "\n";
    }

    // forEach loop - used to iterate over the elements of a container (array, vectors, etc)
    for (string i : cars)
    {s
        cout << i << endl;
    }

    //Initialising Array using user input
    int nums[5];
    cin>>nums[0]>>nums[1]>>nums[2]>>nums[3]>>nums[4];
    cout<<nums[3];

    int arr[5];
    for(int i=0;i<=4;i++){
    cin>>arr[i];
    }
    for(int i=0;i<=4;i++){
    cout<<arr[i]<<" ";
    }*/


    /*// 2D array - [rows][columns]
    int arr[3][5];

    arr[3][5] = 69; // un-Asigned place will have garbage value ex.[3][4] is not asigned value:-898393(random)
    cout << arr[3][4] << endl;

    return 0;
}*/